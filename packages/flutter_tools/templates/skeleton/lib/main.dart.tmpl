import 'package:flutter/material.dart';
import 'package:firebase_core/firebase_core.dart';

import 'src/app.dart';
import 'src/settings/settings_controller.dart';
import 'src/settings/settings_service.dart';

void main() async {
  // Ensure Flutter bindings are initialized before making async calls.
  WidgetsFlutterBinding.ensureInitialized();

  // Initialize Firebase.
  await Firebase.initializeApp(
    options: FirebaseOptions(
      apiKey: "AIzaSyCGHZBobD1Xl-Wv406EI_BeDCJFhdWx4ww",
      authDomain: "cafe-map-77577.firebaseapp.com",
      projectId: "cafe-map-77577",
      storageBucket: "cafe-map-77577.appspot.com",
      messagingSenderId: "355743019196",
      appId: "1:355743019196:web:d3537c6ee13d78ed45a0b9",
      measurementId: "G-QNLDDL76BK" 
    ),
  );

  // Set up the SettingsController, which will glue user settings to multiple
  // Flutter Widgets.
  final settingsController = SettingsController(SettingsService());

  // Load the user's preferred theme while the splash screen is displayed.
  await settingsController.loadSettings();

  // Run the app and pass in the SettingsController. The app listens to the
  // SettingsController for changes, then passes it further down to the
  // SettingsView.
  runApp(MyApp(settingsController: settingsController));
}
